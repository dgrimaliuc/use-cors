{
  "name": "use-cors",
  "version": "0.0.1",
  "description": "Use Cors is a reverse proxy which adds CORS headers to the proxied request. Request URL is taken from the path",
  "author": "denis.grimaliuc@gmail.com",
  "dependencies": {
    "babel-jest": "^29.7.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jest": "^29.7.0",
    "net": "^1.0.2",
    "nodemon": "^3.1.0",
    "save": "^2.9.0",
    "ts-jest": "^29.1.2"
  },
  "devDependencies": {
    "eslint": "^2.2.0"
  },
  "scripts": {
    "lint": "eslint .",
    "deploy": "vercel --prod",
    "vl:start": "vercel dev",
    "dev:start": "nodemon index.js",
    "build": "echo 'No build step required for Node.js server'"
  }
}
